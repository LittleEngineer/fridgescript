#summary Simple loops in FridgeScript.

= Introduction =

A simple [Loops loop] uses an expression to determine how many times the loop should be run.


= Details =

e.g.

{{{
loop( 10 )
{
    // this gets run 10 times
}

loop( a + b )
{
    // this gets run a + b times
}
}}}